import {Card, Text} from 'react-native-paper';import {TouchableOpacity} from 'react-native-gesture-handler';import {StyleSheet} from 'react-native';const useStyles = () => {  return StyleSheet.create({    localizationListItem: {      shadowColor: 'transparent',      borderRadius: 4,      elevation: 'none'    },    localizationListItemContent: {      flexDirection: 'row',      gap: 4    },    localizationListItemCode: {      fontWeight: 700    }  })};const LocalizationListItem = (  {    item,    type,    onValueChange,    navigation  }) => {  const styles = useStyles();  return (    <TouchableOpacity onPress={() => {      onValueChange({city: item[type], airport: type === 'from' ? item.fromAirport : item.toAirport});      navigation.navigate('HomeScreen')    }}>      <Card style={styles.localizationListItem}>        <Card.Content style={styles.localizationListItemContent}>          <Text variant="bodyLarge">{item[type]}</Text>          <Text            variant="bodyLarge"            style={styles.localizationListItemCode}          >            {type === 'from' ? item.fromAirport : item.toAirport}          </Text>        </Card.Content>      </Card>    </TouchableOpacity>  )}export default LocalizationListItem;
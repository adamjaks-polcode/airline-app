import {Snackbar, Text, useTheme} from 'react-native-paper';import {StyleSheet} from 'react-native';const useStyles = () => {  return StyleSheet.create({    wrapper: {      borderRadius: 4,    },    textContent: {      textAlign: 'center'    }  });};const Toast = (  {    text = '',    visible = false,    type = 'default',    onDismiss = () => void 0  }) => {  const styles = useStyles();  const {colors} = useTheme();  return (    <Snackbar      visible={visible}      onDismiss={onDismiss}      style={{...styles.wrapper, backgroundColor: colors[type].light}}    >      <Text style={{...styles.textContent, color: colors[type].dark}}>        {text}      </Text>    </Snackbar>  )}export default Toast;
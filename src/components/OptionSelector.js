import {StyleSheet, View, TouchableOpacity} from 'react-native';import {Text, useTheme} from 'react-native-paper';const useStyles = () => {  return StyleSheet.create({    wrapper: {      flexDirection: 'row',      gap: 4    },    option: {      borderWidth: 2,      borderRadius: 4,      flex: 1,      justifyContent: 'center',      alignItems: 'center',      height: 64    },    optionTitle: {      textAlign: 'center'    }  })};const OptionSelector = (  {    options = [],    selected = '',    onSelectChange = () => {}  }) => {  const theme = useTheme();  const styles = useStyles();  return (    <View style={styles.wrapper}>      {        options.map(option => (          <TouchableOpacity            onPress={() => onSelectChange(option.value)}            style={{flex: 1}}            key={option.value}          >            <View style={{...styles.option, borderColor: selected === option.value ? theme.colors.primary : theme.colors.background.medium}}>              <Text style={styles.optionTitle}>{option.title}</Text>            </View>          </TouchableOpacity>        ))      }    </View>  )}export default OptionSelector;
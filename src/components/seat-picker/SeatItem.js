import {StyleSheet, TouchableOpacity, View} from 'react-native';import {Text, useTheme} from 'react-native-paper';const useStyles = () => {  const {colors} = useTheme();  return StyleSheet.create({    wrapper: {      width: 40,      height: 40,      borderRadius: 4,      justifyContent: 'center',      alignItems: 'center',      borderWidth: 1    },    number: {      color: colors.primary,      fontWeight: 600    }  })};const SeatItem = (  {    item,    onPress = () => void 0  }) => {  const styles = useStyles();  const {colors} = useTheme();  const {isReserved, number, isSelected} = item;  const onSeatStateChange = () => {    if (isReserved) {      return void 0;    }    onPress(number);  };  return (    <TouchableOpacity onPress={onSeatStateChange}>      <View style={        {          ...styles.wrapper,          backgroundColor: isReserved ? colors.danger.light : colors.background.medium,          borderColor: isSelected ? colors.primary : colors.background.medium        }      }>        <Text style={{...styles.number, color: isReserved ? colors.danger.dark : colors.primary}}>{number}</Text>      </View>    </TouchableOpacity>  )};export default SeatItem;
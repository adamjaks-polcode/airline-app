import {createApi, fetchBaseQuery} from '@reduxjs/toolkit/query/react';const baseURL = 'http://192.168.33.8:3000';const endpoint = '/bookings';export const bookingsService = createApi({  reducerPath: 'bookingsServiceApi',  baseQuery: fetchBaseQuery({ baseUrl: baseURL }),  endpoints: builder => ({    getMyBookings: builder.query({      query: () => `${endpoint}/mine`    }),    getBookingsByUserId: builder.query({      query: userId => `${endpoint}/${userId}`    }),    addBooking: builder.mutation({      query: payload => ({        url: `${endpoint}/add`,        method: 'POST',        body: payload,      }),    }),  })});export const { useGetMyBookingsQuery, useGetBookingsByUserIdQuery, useAddBookingMutation } = bookingsService;